services:
  frontend:
    build: .
    container_name: react-vite
    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      CHOKIDAR_USEPOLLING: "true"

    stdin_open: true
    tty: true